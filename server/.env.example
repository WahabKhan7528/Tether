# ───────────────────────────────────────────────────────────────
# Tether — Server Environment Variables
# Copy this file to .env and fill in the values.
# ───────────────────────────────────────────────────────────────

# ── Core ──────────────────────────────────────────────────────
PORT=5000

# MongoDB connection string — REQUIRED
# Local: mongodb://127.0.0.1:27017/tether
# Atlas: mongodb+srv://user:pass@cluster.mongodb.net/tether
MONGODB_URI=

# ── Server & Client URLs ──────────────────────────────────────
SERVER_URL=http://localhost:5000
CLIENT_URL=http://localhost:5173

# ── JWT (cookie-based, always required) ───────────────────────
# Authentication is now always JWT cookie-based. No bypass mode.
# Generate strong secrets (run once):
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# ── Storage Mode ──────────────────────────────────────────────
#   local     → Filesystem (dev only, server/uploads/)
#   imagekit  → ImageKit CDN (recommended — privacy + signed URLs)
#   r2        → Cloudflare R2
STORAGE_MODE=local

# ── ImageKit — required when STORAGE_MODE=imagekit ────────────
# Sign up free at https://imagekit.io
# Dashboard → Developer Options → API Keys
IMAGEKIT_PUBLIC_KEY=
IMAGEKIT_PRIVATE_KEY=
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_imagekit_id

# ── Cloudflare R2 — required when STORAGE_MODE=r2 ─────────────
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=

# ── Runtime ───────────────────────────────────────────────────
NODE_ENV=development
